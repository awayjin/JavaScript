import { sum, multiply } from './01-basic-export'
import * as example from './01-basic-export'
console.log(sum(3, 4))
console.log('multiply:' + multiply(10, 4))
console.log(example)

if (1) {
  // import { multiply } from './01-basic-export'
}